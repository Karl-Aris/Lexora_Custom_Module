<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_payment_provider_form_inherit" model="ir.ui.view">
        <field name="name">payment.provider.form.inherit.fee</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.view_payment_provider_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='module_id']" position="after">
                <field name="payment_fee_percent"/>
                <field name="payment_fee_product_id"/>
            </xpath>
        </field>
    </record>
</odoo>
