<odoo>
    <record id="view_payment_provider_form_inherit_fee" model="ir.ui.view">
        <field name="name">payment.provider.form.inherit.fee</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.view_payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="availability" position="after">
                <group string="Fee Settings" name="fee_settings">
                    <field name="fee_percentage"/>
                    <field name="fee_product_id"/>
                </group>
            </group>
        </field>
    </record>
</odoo>
