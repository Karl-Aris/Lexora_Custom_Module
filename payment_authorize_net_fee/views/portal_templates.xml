<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="authnet_fee_portal_note" inherit_id="payment.portal_payment_status">
        <xpath expr="//div[@id='wrap']//div[contains(@class, 'o_portal_payment_status')]" position="inside">
            <t t-if="tx and tx.provider_id.code == 'authorize_net' and tx.state not in ['done', 'cancel']">
                <div class="alert alert-info" style="margin-top: 1rem;">
                    <strong>Note:</strong> You have selected Authorize.Net as your payment method.
                    <br/>
                    A 3.5% surcharge has been added to your order total.
                    <br/>
                    Please click <strong>“Pay Now”</strong> to complete your payment.
                </div>
            </t>
        </xpath>
    </template>
</odoo>
