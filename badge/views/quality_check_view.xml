<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Inherit the Quality Check form view -->
    <record id="view_quality_check_form_inherit_badge" model="ir.ui.view">
        <field name="name">quality.check.form.inherit.badge</field>
        <field name="model">quality.check</field>
        <field name="inherit_id" ref="quality_control.quality_check_view_form"/>
        <field name="arch" type="xml">
            <!-- Replace or add after the field -->
            <xpath expr="//field[@name='test_type_id']" position="after">
                <field name="x_studio_condition"
                       widget="badge"
                       options="{'terminology': {
                           'Good': 'success',
                           'Damaged': 'danger',
                           'Partial': 'secondary'
                       }}"
                       />
            </xpath>
        </field>
    </record>
</odoo>
