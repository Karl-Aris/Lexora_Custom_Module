<odoo>
<!-- templates.xml -->
<template id="template_product_configuration" name="Product Configuration">
    <t t-call="website.layout">
        <div class="container my-4">
            <h2>Collection: <t t-esc="collection"/></h2>

            <div class="row">
                <!-- Left Column: Image or Placeholder -->
                <div class="col-md-6">
                    <t t-set="has_image" t-value="kits and kits[0].image_1920"/>
                    <t t-if="has_image">
                        <img t-att-src="'data:image/png;base64,%s' % kits[0].image_1920" class="img-fluid" alt="Kit Image"/>
                    </t>
                    <t t-else="">
                        <div class="bg-light d-flex align-items-center justify-content-center" style="height:300px;">
                            <span class="text-muted">No Image</span>
                        </div>
                    </t>
                </div>

                <!-- Right Column: Sizes -->
                <div class="col-md-6">
                    <h5>Select Sizes</h5>
                    <div class="row">
                        <t t-foreach="sizes" t-as="size" t-index="i">
                            <div class="col-3 mb-2">
                                <div class="border p-2 text-center">
                                    <t t-esc="size"/>
                                </div>
                            </div>
                        </t>
                    </div>
                </div>
            </div>
        </div>
    </t>
</template>
</odoo>
