<odoo>
  <record id="view_account_move_form_inherit_salesorderbutton" model="ir.ui.view">
    <field name="name">account.move.form.inherit.sales.order.button</field>
    <field name="model">account.move</field>
    <field name="inherit_id" ref="account.view_move_form"/>
    <field name="arch" type="xml">
      <xpath expr="//div[@name='button_box']" position="inside">
        <button name="action_open_sale_order"
                type="object"
                icon="fa-cube"
                class="oe_stat_button"
                attrs="{{'invisible': [('move_type', '!=', 'in_invoice')]}}">
          <field name="sale_order_count" widget="statinfo" string="Sales Orders"/>
        </button>
      </xpath>
    </field>
  </record>
</odoo>
