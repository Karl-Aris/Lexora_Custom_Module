<odoo>
    <record id="view_order_form_inherit_attachment_selection" model="ir.ui.view">
        <field name="name">sale.order.form.attachment.selection</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Attachment Selections">
                    <field name="attachment_selection_line_ids">
                        <tree editable="bottom">
                            <field name="selected"/>
                            <field name="name" readonly="1"/>
                        </tree>
                    </field>
                    <button name="action_download_selected_attachments"
                            type="object"
                            string="Download Selected Attachments"
                            class="btn btn-primary"/>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
