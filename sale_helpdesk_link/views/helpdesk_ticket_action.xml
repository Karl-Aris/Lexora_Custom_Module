<odoo>
    <record id="action_helpdesk_ticket_by_sale_order" model="ir.actions.act_window">
        <field name="name">Helpdesk Tickets</field>
        <field name="res_model">helpdesk.ticket</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="domain">[('x_matched_sale_order_ids', '=', active_id)]</field>
    </record>
</odoo>
