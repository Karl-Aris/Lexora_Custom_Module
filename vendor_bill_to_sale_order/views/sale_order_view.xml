<odoo>
    <!-- Define the missing action -->
    <record id="action_account_moves_from_sale_order" model="ir.actions.act_window">
        <field name="name">Vendor Bills</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('sale_order_id', '=', active_id), ('move_type', '=', 'in_invoice')]</field>
        <field name="context">{}</field>
        <field name="target">current</field>
    </record>

    <!-- Inherited Sale Order form view -->
    <record id="view_sale_order_form_inherit_vendor_bill" model="ir.ui.view">
        <field name="name">sale.order.form.vendor.bill</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <t t-if="record.state.raw_value == 'sale'">
                    <button name="action_create_vendor_bill"
                            type="object"
                            string="Create Vendor Bill"
                            class="btn btn-primary"/>
                </t>
            </xpath>

            <xpath expr="//sheet" position="before">
                <div class="oe_button_box" name="button_box">
                    <button name="%(action_account_moves_from_sale_order)d"
                            type="action"
                            class="oe_stat_button"
                            icon="fa-file-invoice">
                        <field name="vendor_bill_count" widget="statinfo" string="Vendor Bills"/>
                    </button>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
