<odoo>
    <record id="action_helpdesk_matched_sale_orders" model="ir.actions.act_window">
        <field name="name">Matched Sale Orders</field>
        <field name="res_model">sale.order</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_filter_by_matched_so': 1}</field>
        <field name="domain">[('id', 'in', active_ids)]</field>
    </record>
</odoo>
